% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parse_vsearch_userout.R
\name{parse_vsearch_userout}
\alias{parse_vsearch_userout}
\title{Parse a vsearch `--userout` file into a REGATTA taxonomy table}
\usage{
parse_vsearch_userout(path, id_col = "ASV_id", first_hit_only = TRUE)
}
\arguments{
\item{path}{Path to the vsearch userout file.}

\item{id_col}{Column name to use for the ASV identifier in the output.}

\item{first_hit_only}{If TRUE (default), take only the first row per
ASV. If FALSE, returns every hit.}
}
\value{
A data.frame: `id_col` + 7 rank columns + `pct_id`.
}
\description{
Reads a vsearch `--userout` file, takes the first hit per ASV
(matching the original code's `multiple = "any"` semantics), strips
the sequence prefix off column 2, parses the SINTAX taxonomy via
[parse_sintax()], and returns a clean table with `ASV_id` + the 7
rank columns + `pct_id`. Drop-in as `local_table` to
[reconcile_global_local()] or as a standalone taxonomy table to
[reconcile_checklist()].
}
