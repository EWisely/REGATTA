% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/taxonomize_checklist.R
\name{taxonomize_checklist}
\alias{taxonomize_checklist}
\title{Resolve a regional species checklist to a 7-rank NCBI taxonomy table}
\usage{
taxonomize_checklist(
  input,
  sql_path = .regatta_default_sql_path(),
  prepare_db = !file.exists(sql_path),
  accept_types = c("scientific name", "synonym")
)
}
\arguments{
\item{input}{A path to a checklist file (one-column or with a
\code{Species} column), a character vector of names, or a data.frame
with a \code{Species} / \code{scientific_name} column.}

\item{sql_path}{Path to the local \code{accessionTaxa.sql} taxonomizr DB.
Defaults to the persistent per-user cache shared across REGATTA
(\code{tools::R_user_dir("REGATTA", "cache")}).}

\item{prepare_db}{Build the SQL DB if missing (names+nodes only, via
\code{taxonomizr::prepareDatabase(getAccessions = FALSE)} -- a few hundred MB).}

\item{accept_types}{NCBI name types to accept; default is scientific
name plus recorded synonyms.}
}
\value{
A data.frame, one row per unique input name: \code{input_name},
\code{resolution_status} (\code{"resolved"}/\code{"unresolved"}), \code{name_match_type}
(\code{"scientific name"}/\code{"synonym"}/\code{NA}), \code{taxID}, and the 7 lowercase rank
columns (\code{domain} through \code{species}, \code{NA} where unresolved).
}
\description{
Cleans junk strings (\code{sp.}, \code{spp.}, \code{cf.}, etc.), looks each name up
via \code{taxonomizr} (synonym-aware), and returns a one-row-per-species
table with the 7 lowercase rank columns plus \code{input_name},
\code{resolution_status}, \code{name_match_type}, and \code{taxID}. Run once per
region per taxonomic group; the result is the \code{checklist} input to
\code{\link[=reconcile_checklist]{reconcile_checklist()}}.
}
