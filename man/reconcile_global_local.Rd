% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reconcile_global_local.R
\name{reconcile_global_local}
\alias{reconcile_global_local}
\title{Reconcile global-DB vs. local-DB classifier outputs}
\usage{
reconcile_global_local(
  global_table,
  local_table,
  id_col = "ASV_id",
  global_pct_id_col = "pct_id",
  local_pct_id_col = "pct_id",
  global_pct_id_scale = c("auto", "0-1", "0-100"),
  local_pct_id_scale = c("auto", "0-1", "0-100"),
  Local_advantage = TRUE,
  output_dir = NULL,
  output_prefix = "reconcile_global_local",
  write_summary = TRUE,
  tracking_drop_pattern = "^(MERGED_sample:|obiclean_|seq_rank|ID_STATUS|DEFINITION)"
)
}
\arguments{
\item{global_table}{Taxonomy table from the global-DB classifier:
\code{id_col} + 7 lowercase rank columns + a percent-identity column.}

\item{local_table}{Taxonomy table from the local-DB classifier: same shape.}

\item{id_col}{ASV identifier column name, same in both inputs.}

\item{global_pct_id_col, local_pct_id_col}{Percent-identity column name
in each input.}

\item{global_pct_id_scale, local_pct_id_scale}{One of \code{"auto"} (default),
\code{"0-1"}, or \code{"0-100"}. \code{"auto"} detects 0-1 scale by checking if the
input max is <= 1 and rescales to 0-100 with \code{signif(x, 3) * 100}.}

\item{Local_advantage}{TRUE (default): local wins ties at the
\code{best_pctid} step. FALSE: global wins ties.}

\item{output_dir}{Directory path; default \code{NULL} writes nothing (the
\code{result}/\code{tracking}/\code{stats} list is returned). Supply a directory to also
write \verb{<output_prefix>_taxonomy_table.csv}, \verb{<output_prefix>_tracking.csv},
and (unless \code{write_summary = FALSE}) \verb{<output_prefix>_summary.csv} there.}

\item{output_prefix}{Filename prefix for the output CSVs. Default
\code{"reconcile_global_local"}.}

\item{write_summary}{If \code{TRUE} (default), also write the \verb{_summary.csv}.
\code{\link[=run_regatta]{run_regatta()}} sets this \code{FALSE} so a run writes only one top-level
summary (\code{regatta_summary.csv}).}

\item{tracking_drop_pattern}{Regex matched against input column names;
matching columns are dropped before they enter \verb{$tracking}. Default
strips obitools per-sample matrices and internal bookkeeping but
preserves \verb{BEST_MATCH_*}, \code{COUNT}, \code{TAXID}, \code{BEST_IDENTITY},
\code{NUC_SEQ}, and \code{SCIENTIFIC_NAME}.}
}
\value{
A list with three elements:
\itemize{
\item \code{result}: data.frame with \code{id_col}, then \code{pct_id} (the winning
database's percent identity, kept right after the id for downstream
filtering), then the 7 rank columns. Feeds straight into
\code{\link[=reconcile_checklist]{reconcile_checklist()}}.
\item \code{tracking}: per-ASV decision record with columns grouped per field as
\verb{<field>_global} / \verb{<field>_local} / \verb{<field>_regatta} (the reconciled
value) -- each of the 7 ranks and \code{pct_id} side by side -- then the
decision columns (\code{best_pctid_winner}, \code{preferred_database},
\code{preferred_scientific_name}, \code{global_lca_to_local_triggered}) and any
remaining classifier columns.
\item \code{stats}: (metric, count) data.frame.
}
}
\description{
Given two taxonomy tables for the same set of ASVs -- one from a global
reference database (e.g. obitools + NCBI/EMBL) and one from a local
curated database (e.g. vsearch + a regional reference) -- pick a per-ASV
preferred lineage. The function compares percent identity across the two
databases (the \code{best_pctid} step) and, when global wins but local also
has an assignment, takes the LCA of the two -- labeling it
\code{global_lca_to_local} only when that LCA actually downgrades the call (if
local just agreed to global's depth, it stays a plain global win). Returns a
\verb{$result} taxonomy table
(\code{id_col} + the winning \code{pct_id} + the 7 ranks -- REGATTA exchange format), a
per-ASV \verb{$tracking} audit, and a step-level \verb{$stats} summary; optionally
writes those as three CSVs.
}
