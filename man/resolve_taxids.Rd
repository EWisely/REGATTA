% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resolve_taxids.R
\name{resolve_taxids}
\alias{resolve_taxids}
\title{Convert NCBI taxIDs to a 7-rank taxonomy table}
\usage{
resolve_taxids(
  input,
  taxid_col = NULL,
  sql_path = .regatta_default_sql_path(),
  output_prefix = NULL,
  output_dir = "."
)
}
\arguments{
\item{input}{A numeric/character vector of taxIDs, or a data.frame.}

\item{taxid_col}{Required when \code{input} is a data.frame.}

\item{sql_path}{Path to the local \code{accessionTaxa.sql} taxonomizr DB.
Defaults to the persistent per-user cache shared across REGATTA
(\code{tools::R_user_dir("REGATTA", "cache")}).}

\item{output_prefix}{If non-NULL, writes a CSV with this prefix.}

\item{output_dir}{Directory to write the CSV into.}
}
\value{
The input augmented with the 7 lowercase rank columns
(\code{domain} through \code{species}); all original columns are preserved.
}
\description{
Resolves a column of NCBI taxIDs into a full 7-rank lineage using
\code{taxonomizr}. Accepts either a vector or a data.frame plus \code{taxid_col}.
NA taxIDs become all-NA rows. Optionally writes the augmented table to
disk as \verb{<output_prefix>_full_tax_table.csv}.
}
\examples{
\dontrun{
# Resolve NCBI taxIDs to a 7-rank lineage. Needs the local taxonomizr
# database (built on first use, or pass an existing one via sql_path).
resolve_taxids(c(8030, 8022))

# From a data.frame column, keeping the other columns:
resolve_taxids(data.frame(ASV_id = c("ASV_1", "ASV_2"), TAXID = c(8030, 8022)),
               taxid_col = "TAXID")
}

}
