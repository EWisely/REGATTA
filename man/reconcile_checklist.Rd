% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reconcile_checklist.R
\name{reconcile_checklist}
\alias{reconcile_checklist}
\title{Reconcile a taxonomy table against a regional species checklist}
\usage{
reconcile_checklist(
  taxonomy_table,
  checklist,
  id_col = "ASV_id",
  sql_path = "accessionTaxa.sql",
  output_dir = NULL,
  output_prefix = "reconcile_checklist",
  prior_dir = NULL,
  prior_prefix = "reconcile_global_local",
  tracking_drop_pattern = "^(MERGED_sample:|obiclean_|seq_rank|ID_STATUS|DEFINITION)"
)
}
\arguments{
\item{taxonomy_table}{A data.frame with \code{id_col} + 7 lowercase rank
columns. Additional metadata columns are passed through to \verb{$tracking}
unless they match \code{tracking_drop_pattern}.}

\item{checklist}{A taxonomized regional checklist (output of
\code{\link[=taxonomize_checklist]{taxonomize_checklist()}}) -- a data.frame with the 7 rank columns. A
not-yet-taxonomized checklist (a names-only data.frame, a character
vector, or a path) is accepted too: it is taxonomized on the fly with a
warning, using \code{sql_path}.}

\item{id_col}{ASV identifier column name.}

\item{sql_path}{Path to the local \code{accessionTaxa.sql} taxonomizr DB, used
only if \code{checklist} still needs taxonomizing.}

\item{output_dir}{Directory path; default \code{NULL} writes nothing (the
\code{result}/\code{tracking}/\code{stats} list is returned). Supply a directory to also
write the 3 CSVs there.}

\item{output_prefix}{Filename prefix for the 3 CSVs. Default
\code{"reconcile_checklist"}.}

\item{prior_dir, prior_prefix}{Optional directory of a prior
\code{\link[=reconcile_global_local]{reconcile_global_local()}} output to augment the tracking/summary CSVs
with. Default \code{prior_dir = NULL} (no augmentation); supply the directory
to enable it (only relevant when \code{output_dir} is also set).}

\item{tracking_drop_pattern}{Regex matched against input column names;
matching columns are dropped before they enter \verb{$tracking}.}
}
\value{
A list with \code{result}, \code{tracking}, and \code{stats} elements.
}
\description{
The core REGATTA step. Walks each ASV's lineage species -> domain,
finds the lowest rank present in the regional checklist, and NAs every
rank below that match. Preserves taxonomic specificity where the
checklist supports it; downgrades to a higher rank where it does not.
Accepts either the \verb{$result} output of \code{\link[=reconcile_global_local]{reconcile_global_local()}} or a
standalone classifier-output taxonomy table.
}
\details{
Returns a strict 8-column \verb{$result} (REGATTA exchange format), a per-ASV
\verb{$tracking} before/after audit, and a \verb{$stats} summary; optionally
writes those as three CSVs. If a \code{reconcile_global_local} output folder
is present, the \verb{$tracking} and \verb{$stats} written to disk are \emph{augmented}
versions combining both stages.
}
