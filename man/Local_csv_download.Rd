% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Local_csv_download.R
\name{Local_csv_download}
\alias{Local_csv_download}
\title{Read user-supplied local checklist CSVs}
\usage{
Local_csv_download(loc_csvs, loc_outputname = "Local_Species")
}
\arguments{
\item{loc_csvs}{Character vector of CSV basenames (with `.csv`
extension) in `datasets/`.}

\item{loc_outputname}{Basename (no extension) for the output CSV.}
}
\value{
Invisibly NULL; writes a CSV as a side effect.
}
\description{
Reads one or more CSV files of locally-known species (each must have
`Genus` and `Species` columns), drops `sp.` rows, unites Genus + species
into a single Genus_species string, and writes the deduplicated result
to `datasets/<loc_outputname>.csv`.
}
