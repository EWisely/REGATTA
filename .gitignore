.Rproj.user
.Rhistory
.RData
.Ruserdata
.DS_Store

# Claude Code
CLAUDE.local.md
.claude/settings.local.json
.claude/projects/

..Rcheck/

# GBIF/OBIS download artifacts (regenerable via GBIF_download(); keep out of the repo)
*.zip
[0-9]*-[0-9]*.csv

# Unpublished datasets for local testing/validation ONLY — never commit
# (full Galapagos fish + crustacean datasets; not ours to publish here)
data/

# Generated analysis/comparison write-ups (kept locally, not in the repo)
REGATTA_*.txt
